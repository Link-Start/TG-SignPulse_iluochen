# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

公开开源仓库的自部署用户（包含不太懂技术的新手）。他们在自己的 VPS / NAS 上用 Docker 运行面板，通过浏览器管理自己的多个 Telegram 账号。面板是单管理员使用，没有多用户与角色。

主要使用设备是手机：用户在碎片时间打开面板，确认签到是否正常、修改任务、处理账号失效。PC 仍需完整可用。

## Product Purpose

让 Telegram 多账号的每日签到、定时消息和关键词监听自动运行，并在出问题时让用户快速发现、快速修复。成功标准：用户不用每天手动签到；打开面板几秒内就能判断「今天是否都正常」；账号失效或任务失败时能在手机上当场处理完。

## Positioning

自部署、多账号、以「动作序列」编排签到流程（发文本、骰子、点按钮、AI 识图 / 计算、关键词监听），支持固定 CRON 与时间窗口随机两种调度，以及群组话题级别的签到。

## Operating Context

- 部署：Docker / Docker Compose，FastAPI 同端口托管 Next.js 静态导出的前端，常见访问方式为局域网 IP、Tailscale 或反向代理。
- 手机端高频任务（按优先级）：
  1. 查看今天各任务签到是否成功、下次执行时间
  2. 新建 / 修改任务（选择聊天、编排动作序列、设置调度）
  3. 处理账号失效（手机号验证码或二维码重新登录）
- 其他任务：手动立即执行并看实时日志、查看签到历史与执行日志、系统设置（账号密码、TOTP、AI、全局参数、Bot 通知、Telegram API、备份迁移）、任务剪贴板导入导出。
- 失败通知通过 Telegram Bot 推送，用户常从通知跳转到面板处理。

## Capabilities and Constraints

- 页面：登录、账号列表、任务列表、新建 / 编辑任务、系统设置。
- 账号：手机号验证码登录、二维码登录（含两步验证密码）、独立代理、备注、状态检测（已连接 / 失效需重新登录）、一键巡检、账号日志（保留 3 天）。
- 任务：启用 / 暂停、立即执行（WebSocket 实时日志，断线续传）、签到历史、最近一次执行日志、编辑、删除；每个任务绑定一个账号，包含多个目标聊天，每个聊天有一组动作序列。
- 8 种动作类型；第一个动作必须是发送文本或发送骰子。
- 界面支持中英文切换与深 / 浅色主题切换，需保持。
- 技术约束：Next.js 14 App Router + Tailwind CSS 3，生产环境为 `output: "export"` 静态导出（不能使用服务端能力）；图标库为 `@phosphor-icons/react`；API 调用封装在 `frontend/lib/api.ts`。
- 登录默认凭据为 admin / admin123，首次登录强制跳转设置页修改密码。

## Brand Commitments

- 产品名写法为「TG SignPulse」（仓库名 TG-SignPulse）。
- 名称以外的视觉元素（闪电图标、紫色渐变等）没有保留要求，可整体重做。

## Evidence on Hand

- 无真实用户数据、截图、用户评价或使用统计；设计中不得编造。
- 现有 `frontend/public/favicon.svg`。

## Product Principles

1. 状态先于操作：打开即能判断「今天是否正常」，异常要比正常更醒目。
2. 手机单手可完成核心任务：查看、修改任务、重新登录都不需要回到电脑。
3. 对新手友好：术语给出解释，危险操作有明确确认，错误信息可理解。
4. 自动化工具应安静可靠：界面不喧宾夺主，信息密度服务于快速扫读。
